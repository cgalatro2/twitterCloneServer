const express = require("express");
const mongoose = require("mongoose");
const Comment = mongoose.model("Comment");

const router = express.Router();

// TODO: add like comment route

module.exports = router;
